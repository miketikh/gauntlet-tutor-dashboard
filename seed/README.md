# Seed Scripts

This folder contains seed scripts for populating the Firebase emulators with test data.

## Usage

Seed scripts will be added here as needed. Run them while the Firebase emulators are running.

## Structure

- `seed.js` - Main seed orchestrator (to be created)
- `seed-files/` - Individual seed files for different scenarios
